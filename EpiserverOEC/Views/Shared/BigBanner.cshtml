@model EpiserverOEC.Models.Blocks.BigBanner

<img src="@Url.ContentUrl(Model.Image)" />

<div style="background-image:@Url.ContentUrl(Model.Image)">
    <h1 class="text-center">
        @Html.PropertyFor(x => x.Heading)
    </h1>
</div>
